import React from 'react';

function Footer() {
    return (
        <footer>
            <p>&copy; {new Date().getFullYear()} Hand to Hand Community</p>
        </footer>
    );
}

export default Footer;
